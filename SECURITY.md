# Security

Report a suspected vulnerability through GitHub's private vulnerability-reporting route for this repository when available.

The replay runner accepts only narrowly parsed Python checker and Lean build commands. It rejects shell expansion, pipes, redirects, absolute paths, parent traversal, and unsupported executables. Imported upstream commands are never executed.

Do not submit credentials, private source, private data, or unpublished proof material in a public issue.
